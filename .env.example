# Backend API origin (server-side calls in lib/api.ts and proxy.ts)
BASE_URL="http://localhost:4000"
# Same origin, exposed to the browser where needed
NEXT_PUBLIC_API_URL="http://localhost:4000"

# Absolute site URL used for OG/Twitter metadata (optional — falls back to the
# Vercel production domain, then http://localhost:3000)
# NEXT_PUBLIC_SITE_URL="https://www.example.com"
